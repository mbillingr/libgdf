language: cpp

compiler:
  - gcc
  - clang
  
before_install:
  - sudo apt-get install libboost-dev
  
script:
  - mkdir build
  - cd build
  - cmake .. -DCMAKE_VERBOSE_MAKEFILE=yes
  - make -j
  - make check
